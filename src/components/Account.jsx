import React from "react";

function Account() {

    return (
        <div>
            <h2>Account</h2>
            <p>User Email</p>

            <button>Logout</button>
        </div>
    );
};

export default Account;